{
  "name": "mvx-lang/curl",
  "version": "1.1.0",
  "description": "In-process HTTP for UniData via libcurl/CallC (HTTPGET, HTTPGETFILE) — the faster alternative to curl-cmd; needs gcc + the OS libcurl at install",
  "license": "GPL-2.0-only",
  "systems": ["udt"],
  "provides": ["curl"],
  "dependencies": [],
  "deploy": {}
}
